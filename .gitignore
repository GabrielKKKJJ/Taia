# Credenciais — NUNCA versionar (contem o token do Canvas)
.env

node_modules/
logs/
*.zip
~$*.docx

# Build do painel React: regenerado com `cd painel && npm run build`
painel/dist/

# Entregas dos alunos ficam so no disco local, nunca no repositorio do Taia —
# e trabalho academico de quem usa a ferramenta, nao codigo da ferramenta.
entregas/
